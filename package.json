{
  "name": "threadferry",
  "version": "0.17.0",
  "description": "Safely ferry enterprise chat context to read-only local AI agents.",
  "type": "module",
  "bin": {
    "threadferry": "dist/src/cli.js"
  },
  "files": [
    "dist/src",
    "README.md",
    "POC.md",
    "LICENSE",
    "install.sh",
    "threadferry.example.yaml"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node --eval \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc && chmod +x dist/src/cli.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@wecom/aibot-node-sdk": "1.0.7",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "7.0.2"
  },
  "license": "MIT"
}
